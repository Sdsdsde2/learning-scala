object Main extends App {
  println("Hello, World!")

  // This is a comment in Scala

  // These are Scala variables
  val a = 1
  var b = 2
  // val is used for declaring an immutable variable
  // var is used for declaring a mutable variable

  // In Slaca you can declair variables without declaring their type like so
  val s = "This is a string that I didn't have to say was a string"

  // If statements in Scala
  if (a == 2) {
      // Do something
  } else if (b == 1) {
      // Do something
  }
    else {
      // Do something if none of the if statements are true
  }
}